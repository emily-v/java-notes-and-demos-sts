package comparison;

public interface Fruit {

	// You can have variables in an interface though it is not required
	
	// This is how all variables in an interface
	public static final int y = 1;
	
	// This would always include public static final when compiled
	int x = 100;
}
